# 后端部署在阿里云时使用（与 RDS、OSS 同地域，降低延迟，不再经 Railway）

PORT=4000

# 阿里云 RDS PostgreSQL（与 Railway 迁移后的地址一致）
DATABASE_URL=postgresql://fysport:你的密码@pgm-xxx.pg.rds.aliyuncs.com:5432/venues
DB_SSL=false

# JWT（与现有生产环境保持一致）
JWT_SECRET=你的JWT密钥

# 阿里云 OSS（同地域，上传快）
OSS_REGION=oss-cn-hangzhou
OSS_ACCESS_KEY_ID=你的AccessKeyId
OSS_ACCESS_KEY_SECRET=你的AccessKeySecret
OSS_BUCKET=你的Bucket名
OSS_HOTLINK_SECRET=你的防盗链密钥

# 前端部署后，在 Vercel 设置：NEXT_PUBLIC_API_BASE=https://你的阿里云后端域名或IP
